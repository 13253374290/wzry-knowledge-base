# 王者荣耀全维度知识库 (Honor of Kings) 架构总纲 (AGENTS.md)

> 🛡️ **【第零条：求真与反迎合最高准则 (Supreme Directive)】**
> **AI 的使命是成为真正的人工智能，而非盲从迎合的人工智障。**
> - **严禁无脑盲从**: 绝不为了迎合去做违背客观规律、存在技术硬伤或暗藏系统风险的事情；
> - **实事求是如实报障**: 测试与核验中一旦发现缺陷、报错、卡点或隐患，必须 100% 实话实说还原真实物理现状，严禁为了迎合或盲目乐观而谎报“毫无问题/测试通过”；问题暴露越早代价越小，坚决杜绝粉饰太平；
> - **主动纠偏预警**: 当用户提出的方案存在更科学合理的解法、或按指示执行会引发封禁/崩溃/暗坑时，AI 必须客观、直接指出问题、后果与代价，给出科学最优解，严禁违心附和；
> - **语言极简高效**: 直奔本质，就事论事，坚决去除夸张修饰、虚浮赞美与形式主义废话。

---

## 一、 项目定位与使命 (Mission & Core Values)

本项目（`Honor of Kings Knowledge Base Pipeline`）是专为 **NotebookLM** 及 **大模型 RAG (检索增强生成)** 设计的**王者荣耀全维度结构化知识库工程**。

- **核心使命**：从王者荣耀官网公开接口、动态页面与实战机制中，清洗、关联、补全并生成高保真、零幻觉、结构化的纯文本 Markdown 数据库，专供 Gemini NotebookLM 检索分析。
- **输出目标（统一存放于 `output/` 目录，按王者荣耀真实对局战术逻辑解耦为 6 大维度）**：
  1. `01_王者荣耀_全英雄技能数值与等级成长库.md`（基础四维属性、全技能Lv1-Lv6数值阶梯、冷却消耗、加成公式）
  2. `02_王者荣耀_英雄战术克制与阵容搭档拓扑.md`（最佳搭档体系联动、压制优势、被压制反制天敌分析）
  3. `03_王者荣耀_五大分路定位与实战出装思路.md`（按对抗路/打野/中路/发育路/游走归类，官方推荐出装与推荐原因思路）
  4. `04_王者荣耀_全装备属性与合成升级图谱.md`（全局装备基础属性、唯一被动、双向合成配方路径树与可升级方向）
  5. `05_王者荣耀_全铭文图鉴与英雄搭配方案.md`（五级铭文图鉴、全英雄推荐专属铭文组、30颗满配总属性加成计算）
  6. `06_王者荣耀_峡谷战场机制与宏观运营规则.md`（峡谷兵线、50%补刀收益、打野刀/辅助装游击机制、远古生物羁绊、防御塔机制）

---

## 二、 架构分层规范 (Architecture Layers)

系统全面推行分层与解耦架构，严禁一切业务逻辑、网络请求与静态补丁互相纠缠：

```text
Honor of Kings/
├── AGENTS.md                  # 最高行为准则与架构总纲
├── build.py                   # 统一构建 CLI（支持全量构建或分模块按需构建）
├── requirements.txt           # 基础运行依赖
├── config/                    # [配置与静态字典层]
│   ├── settings.py            # API 端点、请求头、职业/颜色映射枚举
│   ├── item_recipes.py        # 装备合成路径拓扑映射字典 (官方数据缺失字段)
│   └── patches.py             # 官网未收录/延迟上架新英雄数据补丁 (如大司命)
├── src/                       # [核心源码层]
│   ├── core/                  # 公共底层库 (HTTP网络层、数据清洗工具)
│   └── builders/              # 领域知识库构建器 (Hero/Item/Arcana/Rules)
├── output/                    # [生成物交付层] 专供 NotebookLM / RAG 导入的 Markdown
└── scratch/                   # [临时调试与抓包脚本] (禁止在此存放生产代码)
```

### 1. 配置与字典层 (`config/`)
- 官方接口没有提供的关系网（如“装备由哪些小件合成”、“大司命全技能数值”），统一收拢到 `config/`，严禁在业务逻辑中硬编码大段字面量。
- 修改机制或赛季更新时，仅需维护 `config/item_recipes.py` 与 `config/patches.py`。

### 2. 公共核心层 (`src/core/`)
- **网络通信 (`http.py`)**：统一封装带重试、统一 User-Agent、编码自动识别（UTF-8/GBK）的客户端。严禁散落裸调 `requests.get`。
- **文本清洗 (`cleaner.py`)**：严禁 Markdown 中残留 HTML 标签（如 `<br>`, `<p>`, `<font>`, `<span>`），所有富文本必须统一通过正则清洗器转为规范 Markdown。

### 3. 领域构建层 (`src/builders/`)
- 每个 Builder 必须遵循单一职责原则：
  - `HeroBuilder`: 英雄列表获取 -> 详情并发抓取 -> 关联关系解析 -> 格式化输出。
  - `ItemBuilder`: 道具数据抓取 -> 构建双向依赖（合成前置 / 升级后续） -> 格式化输出。
  - `ArcanaBuilder`: 铭文列表抓取 -> 英雄套组匹配 -> 计算 30 颗满配加成 -> 格式化输出。
  - `RulesBuilder`: 结构化维护峡谷机制，提供模块化导出。

### 4. 交付物与产物层 (`output/`)
- 所有生成物默认统一写入 `output/` 目录，避免脏化项目根目录。
- 保证生成的 Markdown 无任何破坏 RAG 检索的乱码、未闭合语法或脏字符。

---

## 三、 数据真实性与补全规范 (Data Integrity)

1. **源头优先**：英雄数值、技能描述、官方推荐出装与铭文，必须以王者荣耀官方公开接口最新数据为准。
2. **受控 Patch 机制**：官方尚未发布新英雄 API 或接口字段延迟时，必须在 `config/patches.py` 中明确标注来源，经人工核验后引入，严禁 AI 凭空捏造虚假数值。
3. **双向拓扑验证**：装备合成必须计算其逆向依赖（“能升级成什么”），确保 RAG 检索“红玛瑙能做什么装备”与“暗影战斧需要什么合成”均能准确命中。
4. **国服英雄基准名册与纵向差集比对机制 (Anti-Leak Diff Engine)**：
   - **纯正国服边界**：严格仅针对王者荣耀国服正式服英雄，坚决排除任何海外服/国际服独立英雄。
   - **基准对照名册 (`config/hero_registry.py`)**：维护国服全量 132 位英雄权威档案（ename, cname, title, 定位），作为全生命周期对比基石。
   - **运行时纵向防漏校验 (`src/core/hero_validator.py`)**：每次抓取必须执行双向差集对比。若官方接口存在延迟/漏抓，必须**实事求是显式报障并自动实施防漏补齐**，绝不容许静默遗漏任何国服英雄；若官方上线新英雄则自动提示合并。

---

## 四、 研发与执行守则 (Engineering Guidelines)

1. **统一入口**：使用 `python build.py --all` 进行一键构建，或通过 `--hero`, `--item`, `--arcana`, `--rules` 按需构建。
2. **并发与反爬安全**：英雄详情采用受控线程池（默认 5-8 线程并发），必须自带延迟或连接复用，绝不滥用高并发轰炸官方服务器导致封禁 IP。
3. **纯净根目录与单一入口**：业务调度统一由 `build.py` 作为全局唯一入口，根目录严禁堆砌散落的业务脚本或裸露数据中间件，保持工程极致纯净。
4. **测试闭环**：任何对 Builder 或 Config 的修改，必须执行本地试运行验证，查看输出文件的行数、体积与结构完整性。

---

## 五、 文件粒度与 AI 认知阈值规范 (File Granularity & Cognitive Thresholds)

为保障 AI 阅读、分析与修改代码时的**最高精准度**，杜绝视窗截断、选择性注意力损耗（Lost in the Middle）与行号定位漂移，本项目所有文件必须遵守以下行数与拆分规范：

| 文件类型 | 黄金适宜行数 | 极限红线 | 约束准则与底层逻辑 |
| :--- | :---: | :---: | :--- |
| **业务源码 (`src/`)**<br>*(Builder、网络层、工具模块)* | **150 ~ 350 行** | **≤ 600 行** | 严格遵守单一职责原则。单个脚本一旦超过 500 行必须立即解耦拆分，确保 AI 单次读取即可在工作记忆中完整建立 AST 语法树与调用流，修改零误差。 |
| **规则与宪法 (`AGENTS.md`)**<br>*(系统准则、开发规范)* | **80 ~ 200 行** | **≤ 300 行** | 防止指令稀释（Instruction Dilution）。高度凝练、结构清晰的核心准则权重最高；严禁堆砌无意义冗长修饰，保持极高执行力。 |
| **静态配置与字典 (`config/`)**<br>*(装备合成树、补丁库)* | **100 ~ 400 行** | **≤ 800 行** | 便于 AI 精准定位并增量维护特定键值对；当数据体量过大时，必须按业务领域横向拆分。 |
| **知识库交付物 (`output/`)**<br>*(NotebookLM Markdown)* | **交付：无需限制**<br>**维护：300 ~ 800 行** | 单篇无上限 | 专供 NotebookLM / RAG 切片检索的交付物允许万行大单文件；但若需 AI 频繁微调或精修，应按章节或英雄拆分为小篇幅文档。 |

