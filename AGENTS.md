# 王者荣耀全维度知识库 (Honor of Kings) 架构总纲 (AGENTS.md)

> 🛡️ **【第零条：求真与反迎合最高准则 (Supreme Directive)】**
> - **严禁无脑盲从**: 绝不迎合做违背客观规律、技术硬伤或暗藏系统风险的操作。
> - **如实报障拒绝粉饰**: 测试中发现缺陷、报错、卡点，100% 还原物理事实，早暴露早解决。
> - **主动纠偏预警**: 方案存在更优解或潜在隐患时，客观直接指出代价并提供最优解。
> - **语言极简高效**: 直奔本质，就事论事，严禁夸张赞美与形式主义废话。

---

## 一、 项目定位与全端交付矩阵

本项目为专供 **NotebookLM / RAG 深度检索** 与 **局内实战推演** 的王者荣耀工业级全维度知识体系：
1. **`output/` (RAG 核心知识库)**：6 大核心维度纯净 Markdown（技能数值、克制搭档、分路出装、装备图谱、铭文图鉴、战场机制）。
2. **`sandbox.html` / `index.html` (推演沙盒)**：单文件纯前端配装推演沙盒，支持全铭文混搭、机制协同算分与连招策略。
3. **`dist_pages/` (云端自动化交付网络)**：Cloudflare 全球 CDN 静态托管（独立域名 `wzry.aodilab.com`，根目录沙盒 + `/download` 苹果风专属提货中心）。
4. **`taobao/pdf/` (出版物矩阵)**：Playwright 高清驱动的 6 册全彩出版级矢量数字手册（内嵌官方透明 PNG 图标与对位头像）。
5. **`miniprogram/` (移动端轻量应用)**：微信原生小程序出装助手。

---

## 二、 架构分层与目录拓扑

```text
Honor of Kings/
├── AGENTS.md                  # 架构总纲与最高行为准则
├── build.py                   # 统一构建 CLI 调度总入口（知识库/沙盒/PDF/小程序）
├── wrangler.json              # 线上交付端网络配置 (wzry.aodilab.com)
├── config/                    # 配置与静态字典（API端点、装备合成树、hero_registry）
│   └── patches/               # 英雄重做/新英雄/变态分支解耦补丁包
├── src/                       # 核心业务源码（业务单一职责，≤350行）
│   ├── core/                  # 网络层 http.py、清洗 cleaner.py、校验 hero_validator.py
│   └── builders/              # 知识库、沙盒、PDF 与小程序多端构建流水线
├── templates/                 # 多端解耦组件模板（≤400行，由 builder 拼装交付）
│   ├── sandbox/               # 沙盒 CSS（主题/面板/商店/槽位）、JS（算分/铭文/连招）
│   ├── pdf/                   # 出版物排版样式（pdf_brand.css、pdf_template.html）
│   ├── xiaohongshu/           # 小红书 Apple 风宣传卡片（common.css、slide1~5）
│   ├── details/               # 淘宝详情页切片样式与模板（details.css、details_slices）
│   └── miniprogram/           # 微信小程序页面与样式模板
├── output/                    # 交付层：专供 RAG 切片分析的纯净 Markdown
├── dist_pages/                # 线上发布层：全功能沙盒与苹果轻量提货门户
├── taobao/                    # 出版物交付层：6 册矢量 PDF、运营文案与资产缓存
└── scratch/                   # 临时调试与抓包脚本（严禁生产代码，随用随清）
```

---

## 三、 数据真实性与治理规范 (SSOT)

1. **源头优先与单一数据源**：英雄与装备基础数据以官网最新接口为准，严禁双数据源割裂维护。
2. **国服边界与动态核验**：
   - 严格限定王者荣耀国服正式服（132+ 位英雄名册 `config/hero_registry.py` 全量核验，支持新英雄上线自动注册 fallback）。
   - 官网延迟或重做技能收拢于 `config/patches/` 模块化维护，严禁凭空捏造数值。
3. **拓扑与机制自洽**：装备具备双向合成拓扑（小件可升级/大件需前置）；算分引擎需具备物法错位惩罚与核心机制协同。

---

## 四、 研发红线与工程认知阈值

| 模块类型 | 适宜行数 | 极限红线 | 约束准则 |
| :--- | :---: | :---: | :--- |
| **业务源码 (`src/`)** | **150 ~ 350 行** | **≤ 500 行** | 单一职责，禁止巨石文件；超过 500 行必须解耦。 |
| **规则总纲 (`AGENTS.md`)**| **70 ~ 100 行** | **≤ 150 行** | 指令高密度，直奔本质，防止指令稀释。 |
| **配置与补丁 (`config/`)** | **100 ~ 300 行** | **≤ 400 行** | 按业务领域横向拆包，便于精准维护。 |
| **前端解耦模板 (`templates/`)**| **150 ~ 300 行** | **≤ 400 行** | 算分、连招、UI 与样式彻底解耦，模板拼装交付。 |
| **交付层 (`output/`, `dist_pages/`)**| **自由生成** | 无上限 | 供切片检索与全球分发，严禁脏 HTML / 乱码。 |

- **统一入口**：统一通过 `python build.py` 构建调度，根目录严禁堆砌散落脚本。
- **自动化部署**：线上发布统一通过 `npx wrangler deploy` CLI 自动推送，严禁手动拖拽上传。
- **高阶视觉与体验红线**：前端统一遵循 Apple 官方纯白银灰极简美学（`#f5f5f7` 底色 + 纯白卡片），严禁大面积沉闷纯黑；提货交互收拢于居中单卡片，去除硬编码测试码，零视觉杂质。
- **环境隔离与纯净交付**：`scratch/` 仅供临时实验，正式交付前彻底清空无用缓存与临时抓包；生产构建零冗余垃圾。
- **测试闭环**：代码或配置修改后必须本地试运行与自动化比对验证，确保零功能倒退。
