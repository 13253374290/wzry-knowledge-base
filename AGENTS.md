# 王者荣耀全维度知识库 (Honor of Kings) 架构总纲 (AGENTS.md)

> 🛡️ **【第零条：求真与反迎合最高准则 (Supreme Directive)】**
> - **严禁无脑盲从**: 绝不迎合做违背客观规律、技术硬伤或暗藏系统风险的操作。
> - **如实报障拒绝粉饰**: 测试中发现缺陷、报错、卡点，100% 还原物理事实，早暴露早解决。
> - **主动纠偏预警**: 方案存在更优解或潜在隐患时，客观直接指出代价并提供最优解。
> - **语言极简高效**: 直奔本质，就事论事，严禁夸张赞美与形式主义废话。

---

## 一、 项目定位与产物

本项目为专供 **NotebookLM / RAG 检索分析** 与 **局内配装推演** 的王者荣耀结构化知识库工程：
- **`output/` (知识库)**：6 大核心维度 Markdown（技能数值、克制搭档、分路出装、装备图谱、铭文图鉴、战场机制）。
- **`sandbox.html` (沙盒工具)**：单文件纯前端配装推演沙盒，支持铭文混搭、机制协同算分与连招策略。

---

## 二、 架构分层与目录拓扑

```text
Honor of Kings/
├── AGENTS.md                  # 架构总纲与最高行为准则
├── build.py                   # 统一构建 CLI 入口（知识库/沙盒按需构建）
├── config/                    # 配置与静态字典（API端点、装备合成树、hero_registry）
│   └── patches/               # 英雄重做/新英雄/变态分支解耦补丁包
├── src/                       # 核心源码（业务单一职责，≤350行）
│   ├── core/                  # 网络层 http.py、清洗 cleaner.py、校验 hero_validator.py
│   └── builders/              # 知识库与沙盒构建器（Hero/Item/Arcana/Rules/Sandbox）
├── templates/sandbox/         # 沙盒前端解耦组件（CSS/HTML/核心算分/连招/协同装配）
├── output/                    # 交付层：专供 RAG 的纯净 Markdown
└── scratch/                   # 临时调试与抓包脚本（严禁生产代码）
```

---

## 三、 数据真实性与治理规范 (SSOT)

1. **源头优先与单一数据源**：英雄与装备基础数据以官网最新接口为准，严禁双数据源割裂维护。
2. **受控 Patch 与国服边界**：
   - 严格限定王者荣耀国服正式服（132位英雄名册 `config/hero_registry.py` 全量核验）。
   - 官网延迟或重做技能收拢于 `config/patches/` 模块化维护，严禁凭空捏造数值。
3. **拓扑与机制自洽**：装备具备双向合成拓扑（小件可升级/大件需前置）；算分引擎需具备物法错位惩罚与核心机制协同。

---

## 四、 研发红线与文件认知阈值

| 模块类型 | 适宜行数 | 极限红线 | 约束准则 |
| :--- | :---: | :---: | :--- |
| **业务源码 (`src/`)** | **150 ~ 350 行** | **≤ 500 行** | 单一职责，禁止巨石文件；超过 500 行必须解耦。 |
| **规则总纲 (`AGENTS.md`)**| **70 ~ 100 行** | **≤ 150 行** | 指令高密度，直奔本质，防止指令稀释。 |
| **配置与补丁 (`config/`)** | **100 ~ 300 行** | **≤ 400 行** | 按业务领域横向拆包，便于精准维护。 |
| **沙盒前端 (`templates/`)**| **150 ~ 300 行** | **≤ 400 行** | 算分、连招、UI 与样式彻底解耦，模板拼装交付。 |
| **知识库交付物 (`output/`)**| **自由生成** | 无上限 | 供 NotebookLM 切片检索，无脏 HTML / 乱码。 |

- **统一入口**：统一通过 `python build.py` 构建调度，根目录严禁堆砌散落脚本。
- **测试闭环**：代码或配置修改后必须本地试运行与自动化比对验证，确保零功能倒退。

